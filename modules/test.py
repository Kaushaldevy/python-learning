import math
import os
import logging

print(math.pow(2,4))
print(math.sqrt(16))
print(math.floor(7/2))
print(math.ceil(7/2))
print(os.path)
logger = logging.getLogger("MAIN")
logger.error("Error happened in the app")
